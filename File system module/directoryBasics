//directory with fs module.

var fs = require('fs');

//making directory
fs.mkdir('newDir', function(){
    fs.writeFile('./newDir/newFile.txt', "Hello WOrld")
});


//remove directory 
fs.unlink('./newDir/mynode.txt', function() {
    fs.rmdir('node');
});

